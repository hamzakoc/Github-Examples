name: Dep Jobs Example

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: stepB
        run: echo "Hello"

  job2:
    needs: job1
    runs-on: ubuntu-latest
    steps:
      - name: stepA
        run: echo "World"
