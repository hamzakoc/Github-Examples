name: Workflow commands

on: [push]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "group logging"
        id: my_group
        run: |
          echo "::group: My Group Message"
          echo "Msg 1"
          echo "Msg 2"
          echo "::endgroup::"
